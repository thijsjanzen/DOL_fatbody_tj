name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps: 
    
    - name: Install GCC
      uses: egor-tensin/setup-gcc@v1

    - uses: actions/checkout@v2
    - name: make
      run: make
