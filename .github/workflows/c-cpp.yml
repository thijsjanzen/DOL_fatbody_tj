name: C/C++ CI

on:
  push:
    branches: [ main dev ]
  pull_request:
    branches: [ main dev ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps: 
    
    - name: Install GCC
      run: sudo apt-get install g++

    - uses: actions/checkout@v2
    - name: make
      run: make
